/** WinRips logo fuchsia — matches `/logo-fixed.png` and favicon. */
export const BRAND_FUCHSIA = "#FF007F";

/** WinRips accent gold — pairs with logo gradient highlights. */
export const BRAND_GOLD = "#e0b034";

/** Primary brand CTA gradient (fuchsia → gold). */
export const BRAND_GRADIENT = `linear-gradient(135deg, ${BRAND_FUCHSIA}, ${BRAND_GOLD})`;
