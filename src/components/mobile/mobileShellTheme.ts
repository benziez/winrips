/** Header / logo chrome — matches WinRips wordmark background. */
export const MOBILE_HEADER_BG = "#111318";

export const mobileHeaderSafePaddingStyle = {
  paddingTop: "calc(env(safe-area-inset-top) + 0.5rem)",
} as const;
