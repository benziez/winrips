/** Initial DOM nodes rendered for long scroll lists (Collection, etc.). */
export const PROGRESSIVE_LIST_INITIAL = 40;

/** Additional items loaded per intersection / Load More tap. */
export const PROGRESSIVE_LIST_BATCH = 30;
